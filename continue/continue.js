var numbers = [45, 75, 85, 95, 15, 52, 900, 58, 23, 83];

for (var i = 0; i < numbers.length; i++) {
    var number = numbers[i];

    if (number > 50) {
        continue;
    }

    // console.log(number);
}